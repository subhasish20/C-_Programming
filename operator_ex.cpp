#include<iostream>
using namespace std;

int main()
{
    int a,b;
    cout<<"enter two number :";
    cin>>a>>b;
    cout<<"if true then it  return 1 else it return 0 ";

    cout<<"\nif a > b :"<<(a>b);
    cout<<"\nif a equal to b :"<<(a==b);
    cout<<"\nif a != b :"<<(a!=b);
    return 0;
}
