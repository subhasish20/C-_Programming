#include<stdio.h>
 int main()
 {
    int n;
    int mid,high=0,low=0,key=0;
    printf("enter :");
    scanf("%d",&n);
    int a[n];
    high=n-1;
    printf("enter the element :");
    for (int i = 0; i < n; i++)
    {
        scanf("%d",&a[i]);
    }
    while (low<=high)
    {
        mid=(low+high)/2;
        if (mid)
        {
            /* code */
        }
        
    }
    
    
    
 }